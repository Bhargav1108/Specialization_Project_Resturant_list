create schema restaurant;
use restaurant;

create table userlogin(userid INT, username varchar(20), pass varchar(20));

create table restaurant(rest_id varchar(10),res_name varchar(30),location varchar(30));